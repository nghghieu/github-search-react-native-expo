import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    backgroundColor: "red",
    padding: 15,
  },
  errorText: {
    color: "#fff",
    fontSize: 15,
    marginBottom: 20,
  },
  errorTextMessage: {
    color: "#fff",
    fontSize: 20,
    fontWeight: "bold",
  },
});

export default styles;
